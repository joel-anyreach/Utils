name               = "attio-instantly-sync"
main               = "src/worker.js"
compatibility_date = "2024-11-01"

# Secrets — set these once via CLI (never stored in this file):
#   wrangler secret put ATTIO_API_KEY
#   wrangler secret put WEBHOOK_SECRET   (optional)
